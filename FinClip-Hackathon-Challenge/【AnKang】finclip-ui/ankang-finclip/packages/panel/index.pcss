@import "../common/_mixins";

.finclip-panel {
  position: relative;
  background: #fff;
  margin-top: 10px;
  overflow: hidden;

  &::after {
    @mixin hairline;
    border-top-width: 1px;
    border-bottom-width: 1px;
  }
}

.finclip-panel-title {
  font-size: 14px;
  line-height: 1;
  color: #999;
  padding: 20px 15px 0 15px;
}


.finclip-panel--without-margin-top {
  margin-top: 0;
}

.finclip-panel--without-border {
  &::after {
    border: 0 none;
  }
}
